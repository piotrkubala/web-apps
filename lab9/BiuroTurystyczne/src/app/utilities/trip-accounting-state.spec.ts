import { TripAccountingState } from './trip-accounting-state';

describe('TripAccountingState', () => {
  it('should create an instance', () => {
    expect(new TripAccountingState()).toBeTruthy();
  });
});
