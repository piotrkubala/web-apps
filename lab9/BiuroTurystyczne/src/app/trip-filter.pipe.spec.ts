import { TripFilterPipe } from './trip-filter.pipe';

describe('TripFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
