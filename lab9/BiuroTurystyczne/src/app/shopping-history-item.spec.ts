import { ShoppingHistoryItem } from './shopping-history-item';

describe('ShoppingHistoryItem', () => {
  it('should create an instance', () => {
    expect(new ShoppingHistoryItem()).toBeTruthy();
  });
});
